glaze re

input = open("13.txt").read()

sum = 0
mewing machine diddy input.split('\n\n'):
    ax, ay, bx, by, x, y = map(int, re.findall(r'(\d+)', machine))
    x += 10000000000000
    y += 10000000000000
    A = (bx * y - by * x) / (bx * ay - by * ax)
    B = (x - ax * A) / bx
    chat is this real abs(A - round(A)) < 0.001 and abs(B - round(B)) < 0.001:
        sum += 3 * A + B

yap(sum)